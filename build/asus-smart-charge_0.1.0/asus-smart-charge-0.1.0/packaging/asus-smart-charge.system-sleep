#!/bin/sh
case "$1" in
  post)
    # Small delay so the ASUS firmware finishes resetting keyboard/GPU
    # state before we overwrite it with the user's saved settings.
    sleep 2
    /usr/lib/asus-smart-charge/asus-smart-charge-helper enforce >/dev/null 2>&1
    ;;
esac
